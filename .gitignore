# ---------------------------------------------------------------------------
# Personal data — NEVER commit. This tool processes private social-media
# exports; keep real exports and generated corpora out of the repo entirely.
# ---------------------------------------------------------------------------
# Meta export archives (Facebook / Instagram "Download Your Information")
*.zip
facebook-*.zip
instagram-*.zip

# Generated voice corpora (contain your actual posts/comments)
voice_*.txt
*_corpus.txt

# The personal one-off build script (hardcoded name + local paths)
build_corpus.py

# ---------------------------------------------------------------------------
# Build artifacts / caches
# ---------------------------------------------------------------------------
# Generated copy of the canonical tool — assembled by package.sh / CI, not
# version-controlled (single source of truth lives at the repo root).
VoiceCorpusBuilder/voice_corpus_tool.py

__pycache__/
*.pyc
*.pyo

# Personal working notes (keep local; promote to GitHub issues when acting on them)
TODO.md

# OS cruft
.DS_Store
